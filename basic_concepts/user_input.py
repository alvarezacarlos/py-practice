# The method is a bit different in Python 3.6 than Python 2.7.
# Python 2.7 uses the raw_input() method. While, Python 3.6 uses the input() method.

username = input('Enter the username: ')
print('usernae is: ' + username)