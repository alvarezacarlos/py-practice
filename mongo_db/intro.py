# Python can be used in database applications. One of the most popular NoSQL database is MongoDB. MongoDB stores data in JSON-like documents, which makes the database very flexible and scalable.

# Python needs a MongoDB driver to access the MongoDB database. you can use the MongoDB driver "PyMongo".

#install pymongo
# pip3 install pymongo

import pymongo # test if pymongo has been installed by writing this line and execute the this script
